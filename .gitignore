.DS_Store
/myworktree
/mw
myworktree_netgo
*.log
.gitnexus
.claude/
.archive/
/AGENTS.md
/CLAUDE.md
.omo/
.codewhale/
# Agent project-level state: reasonix (and similar agents) create a .reasonix/
# under the working directory (e.g. .reasonix/tasks/ for background jobs).
# This is runtime state, not source — keep it out of the repo.
.reasonix/
.playwright-mcp/

# Local Go build cache (sandbox GOCACHE) — build artifact, never commit.
.gocache/
